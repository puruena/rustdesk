name: RustDesk Build 2026 Fix

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-linux:
    runs-on: ubuntu-22.04
    
    # Forzamos Node 24 para evitar el error de deprecación de GitHub
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y binutils-dev build-essential curl git libasound2-dev \
          libcap-dev cmake pkg-config libdbus-1-dev libgstreamer-plugins-base1.0-dev \
          libgstreamer1.0-dev libgtk-3-dev liblzma-dev libpam0g-dev libpango1.0-dev \
          libpulse-dev libva-dev libvdpau-dev libvpx-dev libx11-dev libxcb-randr0-dev \
          libxcb-shape0-dev libxcb-xfixes0-dev libxdo-dev libxfixes-dev libxi-dev \
          libxkbcommon-dev libxrandr-dev libxtst-dev llvm-dev nasm ninja-build \
          unzip wget yasm libclang-dev libunwind-dev

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install vcpkg (Classic Mode Force)
        run: |
          if [ ! -d "vcpkg" ]; then
            git clone https://github.com/microsoft/vcpkg
            ./vcpkg/bootstrap-vcpkg.sh
          fi
          # ELIMINAMOS el vcpkg.json temporalmente para que NO entre en modo manifiesto
          # Esto evita el error "In manifest mode, vcpkg install does not support individual package arguments"
          if [ -f "vcpkg.json" ]; then rm vcpkg.json; fi
          
          echo "VCPKG_ROOT=$(pwd)/vcpkg" >> $GITHUB_ENV
          ./vcpkg/vcpkg install libvpx libyuv opus aom --classic

      - name: Prepare Bridge & Dependencies
        run: |
          cargo install flutter_rust_bridge_codegen --version 1.80.0
          export PATH="$HOME/.cargo/bin:$PATH"
          flutter pub get
          # Si el archivo main no está en src/main.rs, esta línea fallará pero el script seguirá
          flutter_rust_bridge_codegen --rust-input src/main.rs --dart-output lib/bridge_generated.dart || echo "Bridge ya generado"

      - name: Build Flutter App (Release)
        run: |
          flutter clean
          flutter build linux --release

      - name: Upload Finished Binary
        uses: actions/upload-artifact@v4
        with:
          name: RustDesk-Linux-OK
          path: build/linux/x64/release/bundle/
